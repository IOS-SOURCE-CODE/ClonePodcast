platform :ios, '9.0'

target 'ClonePodcast' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
   pod 'Alamofire', '~> 4.7'
   pod 'SDWebImage', '~> 4.0'
   pod 'FeedKit', '~> 8.0'
  # Pods for ClonePodcast

end
